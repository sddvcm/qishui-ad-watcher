# 汽水音乐自动看广告 功能说明

## 一、这个程序是干什么的

在电脑上，通过 **vivo 办公套件**（或其他支持投屏到电脑的软件）把手机屏幕投屏到电脑，程序会自动识别汽水音乐 App 的广告界面，**自动点击看广告**，帮你自动领取听歌时长或金币。

整个过程全程自动化：识别界面、点击按钮、处理广告，都不用人工操作。

---

## 二、功能介绍

在福利中心点击**"领时长"（或"继续领"）**任务后，自动帮你观看视频/直播广告，完成后自动点击领取**免费听歌时长**，自动进入下一个广告循环执行，全程无需人工干预。

---

## 三、运行环境要求

- **操作系统**：Windows 10 或 Windows 11（64 位）
- **无需安装 Python**，解压即可运行
- **需要投屏软件**：vivo 办公套件（或能投屏手机到电脑、窗口标题含"投屏"字样的软件）
- **需要汽水音乐 App** 安装并登录在手机上

**说明**：程序识别手机投屏窗口，对手机系统无限制；但推荐使用 vivo 手机自带套件，兼容性最好。

---

## 四、使用方法

### 第一步：准备工作

1. 打开 vivo 办公套件，把手机连接到电脑并**投屏**
2. 在手机上打开汽水音乐，进入**福利中心**，停在"领时长"任务页面
3. 确认电脑上能看到"手机投屏"窗口

### 第二步：启动程序

1. 进入 `汽水音乐自动看广告` 文件夹
2. 双击 `汽水音乐自动看广告.exe`
3. 程序会自动锁定"手机投屏"窗口，开始自动看广告

### 第三步：观察运行

- 程序主界面（控制面板）会实时显示**运行日志**，能看到每一步动作
- 底部有**统计信息**：已看广告数、取消直播数、退出直播间数、错误数、运行时长

---

## 五、控制面板按钮说明

界面顶部的控制按钮：

| 按钮 | 作用 |
|:---|:---|
| **开始运行** | 开始自动看广告 |
| **暂停 / 恢复** | 暂停后继续运行，随时可切换 |
| **停止** | 结束本次运行 |
| **保存配置** | 把当前设置保存，下次启动自动加载 |
| **测试截图+OCR** | 截取当前画面并识别文字，用于验证识别是否正常 |
| **列出窗口** | 列出电脑上所有窗口，确认能找到投屏窗口 |

> 提示：如果日志提示"未找到窗口"，先点**列出窗口**看投屏窗口标题，再到"窗口标题关键词"里填对应的关键词。

---

## 六、常用参数说明（左侧设置面板）

> 一般情况下**全部使用默认值即可**，以下参数供需要微调时使用。

| 参数 | 默认值 | 说明 |
|:---|:---|:---|
| 窗口标题关键词 | 投屏 | 要锁定哪个窗口，默认匹配"手机投屏"。不要填"汽水"，否则会匹配到本程序自己的窗口 |
| 扫描间隔（秒） | 0.3 | 检测画面的频率，越小响应越快（直播广告只给5秒，必须快） |
| 广告数量上限 | 0 | 看满多少个广告后**自动暂停**（0=不限，一直刷） |
| 最大广告数超时 | — | 单个广告等待完成的超时时间 |

### 点击位置参数

| 参数 | 默认值 | 说明 |
|:---|:---|:---|
| 领取成功X偏移 | -5 | 视频广告看完，点右上角"领取成功"右侧关闭按钮的偏移 |
| 直播取消偏移 | -10 | 直播广告出现时点"取消"按钮的偏移 |
| 直播间X-观众数偏移 | 40 | 误进入直播间后，通过右上角观众数定位退出按钮 |
| 直播间X-固定比例 | 0.9 / 0.1 | 找不到按钮时的兜底位置（宽90%、高10%） |
| 直播间退出超时 | 20 | 进入直播间后多久没退出就算异常 |

> 如果程序点击位置不准确，主要调整这几个偏移参数；通常哪类广告点不准就调对应的那一个。

---

## 七、自动看广告的流程

程序不会盲目乱点，而是按下面的逻辑智能判断当前画面来决定下一步：

### 视频广告流程

1. 点击"领时长 / 继续领"打开广告
2. 等待广告右上角**倒计时结束**
3. 检测到"领取成功" → 点击右侧关闭按钮
4. 弹出奖励提示 → 点击"领取奖励"
5. 自动进入下一个广告，循环执行

### 直播广告流程

1. 打开直播广告后，画面**中间**会弹出"5秒后进入直播间"
2. 程序在 5 秒内**快速点击"取消"**，避免进入直播间
3. 取消后继续按视频广告流程处理（等倒计时、领取奖励）

> 直播广告必须在 5 秒内点掉"取消"，这是整个流程里要求最高的一步，所以程序用很短的扫描间隔来保证及时响应。

### 异常自动处理（自动恢复不卡死）

程序内置了异常恢复机制，即使遇到意外情况也能自己回到正常流程：

- **误进了直播间** → 自动点右上角 X 退出，并处理"退出不看了"弹窗
- **遇到"第X日免费听"解锁弹窗** → 自动点击"继续观看 / 领取奖励"继续
- 广告卡住 / 超时 → 自动回到初始状态重新开始

所以就算偶尔弹出的广告形式不一样，程序也能自动适应，不需要你手动干预。

---

## 八、常见问题

**Q：日志显示"未找到包含 '投屏' 的窗口"怎么办？**
点控制面板的"列出窗口"，看投屏窗口的真实标题，把"窗口标题关键词"改成匹配的关键词再试。

**Q：程序点了开始但没在操作手机画面？**
确认投屏窗口在最前面没有被其他窗口遮挡（程序需要窗口可见才能截图）。

**Q：广告点不准确，点歪了？**
对应调整第五节里对应的"点击位置偏移"参数，多试几次。

**Q：直播广告点取消老是来不及 / 点成"进入直播间"？**
把"扫描间隔"调小（如 0.2），并确认投屏画面清晰、不要卡顿。



**Q：安装说明？**
绿/杀毒软件可能误报。本程序是 PyInstaller 打包的 Python 程序，若被杀毒软件拦截，需添加信任后运行。

---

## 九、免责说明

- 本程序仅限**个人自用**，用于自动化点击软件内正常提供的广告任务
- 自动化操作存在被平台风控、账号处罚的风险，请知悉并自行判断使用
- 汽水音乐每次版本更新，按钮位置和文字可能变化，程序可能需要相应调整