# 汽水音乐自动看广告脚本 — 详细开发文档

> 项目名：**qishui-ad-watcher**
> 本文档用于把本项目完整交付给另一名 AI Agent 重新实现。按本文档实现即可得到功能等价的可运行程序（目标版「领时长版」，不含已删除的「赚金币版」）。
>
> 配套运行代码必须实现的核心文件：
> - `qishui_ad_watcher.py`（核心：窗口/截图/OCR/状态机/点击）
> - `qishui_gui.py`（PyQt5 控制面板）
> - `启动GUI.bat`（一键启动）
> - `requirements.txt`（依赖）

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术原理与选型](#2-技术原理与选型)
3. [文件结构与职责](#3-文件结构与职责)
4. [依赖清单与安装](#4-依赖清单与安装)
5. [配置参数全表（Config）](#5-配置参数全表config)
6. [模块详解：日志与编码](#6-模块详解日志与编码)
7. [模块详解：鼠标点击工具](#7-模块详解鼠标点击工具)
8. [模块详解：OCR 引擎（OCREngine）](#8-模块详解ocr-引擎occrengine)
9. [模块详解：窗口管理与截图](#9-模块详解窗口管理与截图)
10. [状态机完整设计](#10-状态机完整设计)
11. [状态流转与各状态处理详解](#11-状态流转与各状态处理详解)
12. [关键 OCR 关键词清单](#12-关键-ocr-关键词清单)
13. [点击位置策略详解](#13-点击位置策略详解)
14. [调试与测试功能](#14-调试与测试功能)
15. [GUI 控制面板（PyQt5）架构](#15-gui-控制面板pyqt5架构)
16. [启动批处理规范](#16-启动批处理规范)
17. [命令行接口](#17-命令行接口)
18. [已踩坑的兼容性要点](#18-已踩坑的兼容性要点)
19. [可执行文件打包（PyInstaller）](#19-可执行文件打包pyinstaller)
20. [测试验证清单](#20-测试验证清单)
21. [重写完备性检查清单](#21-重写完备性检查清单)

---

## 1. 项目概述

**目标**：自动观看「汽水音乐」App（字节跳动旗下音乐应用）福利中心的广告，自动领取「免费听歌时长」。

**使用场景**：用户用 vivo 手机「办公套件」把手机屏幕**投屏到电脑**，脚本锁定电脑上的投屏窗口，通过「截屏 → OCR 识别文字 → 状态机判断 → 模拟鼠标点击」的循环来自动完成整个广告领取流程。**不需要 ADB、不需要连接手机。**

**完成的标准流程**：
1. 识别并点击「领时长」或「继续领」入口按钮 → 打开一个广告（视频广告或直播广告）。
2. **视频广告**：等待右上角倒计时结束，识别到「领取成功」→ 点击其右侧的 X 关闭按钮 → 弹出「领取奖励」→ 点击 → 自动进入下一个广告。
3. **直播广告**：广告打开后几秒，屏幕中央出现「X 秒后进入直播间」+「取消」按钮（仅约 5 秒倒计时，须快速检测）。检测到后立即点击「取消」→ 随后按视频广告流程，等「领取成功」→ X →「领取奖励」→ 下一个广告。
4. 循环执行，直到达到设定广告数上限或用户停止。

**容错**：若在直播广告因未及时点「取消」而误入直播间，脚本会自动切换到「退出直播间」流程，点右上角 X 退出，并处理退出后弹出的「退出不看了」推荐弹窗。

---

## 2. 技术原理与选型

| 环节 | 方案 | 理由 |
|:---|:---|:---|
| 窗口截图 | **mss**（屏幕区域截图） | vivo 投屏窗口内容是视频流渲染，`PrintWindow` API 抓不到视频帧，**必须用 mss 对屏幕区域（窗口矩形）前台截图**。截图前先 `SetForegroundWindow` 把目标窗口置前。 |
| OCR | **RapidOCR**（`rapidocr_onnxruntime`） | 纯本地离线条形识别，无需联网 API，识别中文效果好，支持置信度与文本框。 |
| 鼠标模拟 | **win32 `SetCursorPos` + `mouse_event`** | 在屏幕绝对坐标模拟真实鼠标左键点击。 |
| 键盘热键 | **keyboard** | 注册 ESC 停止脚本。 |
| GUI | **PyQt5** | 提供可视化配置、实时日志、开始/暂停/停止。 |
| 状态管理 | 手写**状态机** | 避免复杂 if 嵌套，清晰表达广告流程的多个阶段。 |

---

## 3. 文件结构与职责

```
项目目录/
├── qishui_ad_watcher.py    # 核心：Config、OCREngine、窗口管理、截图、AdWatcher 状态机、命令行入口
├── qishui_gui.py           # PyQt5 GUI 控制面板，导入核心模块并在线程中运行
├── 启动GUI.bat             # Windows 一键启动（UTF-8 编码 + chcp 65001）
├── requirements.txt        # Python 依赖清单
├── 开发文档.md             # 本文档
├── 功能说明.md             # 面向使用者的操作手册
├── gui_config.json         # 【运行时生成】GUI 保存的配置
├── qishui_ad_watcher.log   # 【运行时生成】日志
└── debug_screenshots/      # 【运行时生成】调试截图 + OCR 文本
```

`qishui_gui.py` 通过 `import qishui_ad_watcher as core` 复用核心模块，二者必须同目录。

---

## 4. 依赖清单与安装

**requirements.txt 内容（必须严格一致）：**

```
rapidocr-onnxruntime>=1.4.4
pywin32>=306
keyboard>=0.13.5
mss>=9.0.1
Pillow>=9.0.0
numpy>=1.21.0
PyQt5>=5.15.0
```

安装：`pip install -r requirements.txt`

**注意**：`rapidocr-onnxruntime` 首次运行时需下载 OCR 模型（含 onnxruntime 推理引擎），离线环境可能失败，应保证首次运行联网。

---

## 5. 配置参数全表（Config）

`Config` 是一个 `@dataclass`，字段及含义如下（默认值与作用必须与核心代码一致）：

```python
@dataclass
class Config:
    window_title: str = "投屏"      # 窗口标题关键词（不区分大小写）；太宽泛会误匹配
    scan_interval: float = 0.3      # 扫描间隔（秒）；直播广告仅5秒倒计时，必须小
    ad_timeout: int = 120           # 广告超时（秒），超时返回到空闲态
    click_delay: float = 0.3        # 点击后的延迟（秒）
    ad_load_wait: float = 3.0       # 广告加载等待（秒），点入口按钮后等待
    reward_timeout: int = 15        # 奖励弹窗超时（秒）
    x_button_offset: int = -5       # 视频广告：点"领取成功"右侧X按钮的偏移（OCR常连读，X贴近文本右缘，负值向左微调）
    live_cancel_offset: int = -10   # 直播广告：取消按钮偏移（OCR把"4s后进入直播间取消"识别成整体时点右边缘用）
    live_x_viewer_offset: int = 40  # 直播间X：观众数右侧偏移（像素）
    live_x_follow_offset: int = 200 # 直播间X："关注"按钮右侧偏移（像素）
    live_x_fixed_x_ratio: float = 0.90  # 直播间X：兜底固定位置，窗口宽度比例
    live_x_fixed_y_ratio: float = 0.10  # 直播间X：兜底固定位置，窗口高度比例
    max_image_width: int = 1000     # OCR前最大图片宽度，超过则等比例缩放提速
    region_top_right: tuple = (0.55, 0.0, 1.0, 0.18)   # 右上角区域（检测"领取成功"、倒计时）
    region_center: tuple = (0.1, 0.25, 0.9, 0.75)      # 中心区域（检测"进入直播间"/"取消"）
    region_live_close: tuple = (0.75, 0.03, 1.0, 0.12) # 直播间X按钮区域（保留字段）
    live_room_timeout: int = 20     # 直播间退出超时（秒）
    debug: bool = True              # 是否保存调试截图
    debug_dir: str = "debug_screenshots"
    max_ads: int = 0                # 广告数量上限（0=无限，达到后自动停止）
    dry_run: bool = False           # 试运行模式：检测但不点击
```

区域比率均为 `(x_start, y_start, x_end, y_end)`，表示相对整张截图的归一化坐标。

---

## 6. 模块详解：日志与编码

- **控制台编码**：在 Windows 上，将 `sys.stdout`/`sys.stderr` `reconfigure(encoding='utf-8', errors='replace')`，配合启动脚本 `chcp 65001`，保证中文不乱码。
- **DPI 感知**：程序开头调用 `ctypes.windll.user32.SetProcessDPIAware()`，确保高分屏下 `GetWindowRect` 与鼠标坐标一致（不做会点偏）。
- **logging 配置**：
  ```python
  logging.basicConfig(level=logging.INFO,
      format='%(asctime)s [%(levelname)s] %(message)s',
      datefmt='%H:%M:%S',
      handlers=[logging.StreamHandler(sys.stdout),
                logging.FileHandler('qishui_ad_watcher.log', encoding='utf-8')])
  logger = logging.getLogger("qishui")
  ```
- **可选依赖容错**：`rapidocr_onnxruntime` 和 `keyboard` 用 `try/except ImportError` 决定 `HAS_RAPIDOCR` / `HAS_KEYBOARD`。没有 keyboard 时提示用 Ctrl+C。

---

## 7. 模块详解：鼠标点击工具

```python
MOUSEEVENTF_LEFTDOWN = 0x0002
MOUSEEVENTF_LEFTUP = 0x0004

def mouse_click(x, y):
    # 屏幕绝对坐标点左键
    ctypes.windll.user32.SetCursorPos(x, y)
    time.sleep(0.02)
    ctypes.windll.user32.mouse_event(MOUSEEVENTF_LEFTDOWN, 0, 0, 0, 0)
    time.sleep(0.05)
    ctypes.windll.user32.mouse_event(MOUSEEVENTF_LEFTUP, 0, 0, 0, 0)
```

另提供 `mouse_click_postmessage(hwnd, x, y)` 用 `PostMessage` 后台点击（本项目当前未强制启用，作为可选能力保留）。

---

## 8. 模块详解：OCR 引擎（OCREngine）

```python
class OCREngine:
    def __init__(self, config):
        # 无 rapidocr 时 raise RuntimeError(提示 pip install)
        self.engine = RapidOCR()   # 构造时打印"加载 OCR 模型..."

    def detect(self, image) -> List[dict]:
        # 若宽度 > max_image_width 则按比例缩放（提速）
        # engine(np_array) 返回 (result, elapse)；result 为空则返回 []
        # 每条解析为：
        #   { text, x, y, w, h,
        #     center: (cx,cy), right: (rx,cy), score }
        # 注意：box 是4角点 [[x1,y1],...]，坐标要除以 scale 还原到原图

    @staticmethod
    def find_text(results, target) -> Optional[dict]:
        # 全程模糊包含匹配：`target in r['text'] or r['text'] in target`

    @staticmethod
    def find_text_in_region(results, target, region, img_size) -> Optional[dict]:
        # 先把 region 归一化比率转为像素范围，再在范围内做 find_text（按中心点在区域内判断）
```

实现要点：`print(Image,Lanczos)` 缩放；中心点取文本框几何中心，右边缘取 `(x_max, cy)`。

---

## 9. 模块详解：窗口管理与截图

- `find_windows(keyword)`：用 `win32gui.EnumWindows` 收集「可见且标题包含关键词（忽略大小写）」的窗口，返回 `[(hwnd, title)]`。
- `list_all_windows()`：列出全部可见窗口。
- `get_window_rect(hwnd)`：返回 `(left, top, width, height)`。
- `capture_window_mss(hwnd)`：用 `mss.mss()` 抓取窗口矩形屏幕区域，转 PIL Image。
- `capture_window_printwindow(hwnd)`：用 `PrintWindow`+`PW_RENDERFULLCONTENT` 抓取（保留，但**不可用于视频流**）。
- `capture_window(hwnd)`：**实际使用入口**。先 `ShowWindow(SW_RESTORE)` + `SetForegroundWindow`，`sleep(0.05)`，再调 `capture_window_mss`。

> 关键经验：vivo 投屏窗口内容为视频流，`PrintWindow` 抓空白，必须用 mss 前台截图。

### 目标窗口查找与自身排除（AdWatcher.find_target_window）

```python
EXCLUDE_TITLE_KEYWORDS = ("控制面板", "命令提示符", "Windows PowerShell", "cmd.exe", "Terminal")

def find_target_window(self) -> bool:
    results = find_windows(config.window_title)
    if not results:                      # 打印错误 + 列全部可见窗口 + 返回 False
        ...
    # 过滤掉标题命中最排除关键词的"脚本自身窗口"
    filtered = [非自身窗口]; excluded = [自身窗口]
    if not filtered:                     # 全部被排除 -> 报错提示改更精确关键词，返回 False
        ...
    # 在 filtered 里选面积最大的窗口作为目标（投屏窗口通常最大）
    ...
    self.hwnd = best_hwnd
    logger.info(f"已锁定窗口: {best_title}")
    return True
```

> 重要：此功能解决过「窗口关键词=汽水」时误锁到 GUI 自己窗口的问题。GUI 标题是「汽水音乐自动看广告 - 控制面板」，命中"控制面板"被排除。

`capture_and_ocr()`：调 `capture_window` 得到截图和窗口位置，执行 `ocr.detect`，保存到 `last_screenshot`/`window_pos`，返回 `(results, screenshot)`。

---

## 10. 状态机完整设计

状态常量：

```python
class State:
    IDLE         = "空闲"
    AD_PLAYING   = "广告播放中"
    LIVE_ROOM    = "直播间内"
    REWARD_POPUP = "奖励弹窗"
```

**主循环 `run()`**：
1. `running=True`，记录 `start_time`。
2. `find_target_window()` 失败则直接返回。
3. 窗口置前，`sleep(1)`。
4. 打印启动横幅（含 ESC 提示、试运行提示）。
5. `while running:` 循环：`paused` 则 `sleep(0.5)` 跳过；否则 `self.tick()`。异常被捕获，`errors += 1`，`sleep(2)`。
6. 退出后 `print_stats()`。

**单次 `tick()` 的顺序（严格遵循）**：
1. **广告数上限检查**：`max_ads>0 and ads_watched>=max_ads` → 打印并 `running=False`，return。
2. 截图 + OCR + `save_debug`。
3. **特别弹窗检测（最高优先级）**：调 `_handle_continue_watching_popup`，命中则置 `AD_PLAYING`、重置计时与 `_live_cancelled_this_ad`、`sleep(ad_load_wait)`、return。
4. 按当前状态分发到 `_handle_idle` / `_handle_ad_playing` / `_handle_live_room` / `_handle_reward_popup`。

---

## 11. 状态流转与各状态处理详解

### 11.1 `_handle_continue_watching_popup`（特别弹窗）

触发背景：汽水音乐会弹「第 X 日免费听」解锁弹窗，特征为灰色链接 **"坚持退出"**，主按钮有两种：
- 「继续观看」（文案形如「再看08秒继续解锁」）
- 「领取奖励」（文案形如「再看3个视频提前得」）

逻辑：
1. `find_text(results, "坚持退出")` 不存在 → 返回 `False`（不处理）。
2. 优先 `find_text("坚持退出")` 后找 `"继续观看"`，找不到再找 `"领取奖励"`（`continue_btn`）。仍找不到 → 记 warning 并 `return False`。
3. 找到 → 记录日志「检测到'坚持退出'免费解锁弹窗，点击'{btn_text}'按钮」，`click_ocr_center` 点击主按钮，返回 `True`。

> 用「坚持退出」作为**唯一触发特征**，避免与正常奖励弹窗的「领取奖励」混淆；正常奖励弹窗无「坚持退出」。

### 11.2 `_handle_idle`（空闲）

按序判断（命中即处理并 return / sleep）：
1. 找入口按钮：`find_text("领时长")`，找不到用 `find_text("继续领")`。命中 → 日志「找到入口按钮'...'，点击开始观看广告」，`click_ocr_center`，置 `AD_PLAYING`、重置计时，`sleep(ad_load_wait)`，return。
2. `find_text("领取成功")` → 已在广告中，切 `AD_PLAYING`。
3. `find_text("领取奖励")` → 切 `REWARD_POPUP`。
4. `find_text("可领取")` 或 `find_text("秒后")` → 广告在播放，切 `AD_PLAYING`。
5. `find_text("说点什么")` 或 `find_text("本场点赞")` → 误入直播间，切 `LIVE_ROOM` 并重置计时。
6. 都未命中 → 日志「空闲中...等待'领时长'按钮出现」，`sleep(scan_interval)`。

### 11.3 `_handle_ad_playing`（广告播放中）

1. 超时检查：超 `ad_timeout` → 置 `IDLE`。
2. **优先级0 误入直播间检测**：`in_live_room = ("说点什么") or (("关注") and ("本场点赞" or "直播中"))`，且**不**含 `"领取成功"` → 判定已进入直播间，切 `LIVE_ROOM`，重置计时，return。
3. **优先级1 直播广告取消**（仅当 `not _live_cancelled_this_ad`）：
   - 在 `region_center` 内找 `"取消"` → `cancel_result`；再在其区域找 `"进入直播间"` → 二律都命中才处理。
   - 命中则：
     - 若 `"进入直播间" in cancel_text or "后进入" in cancel_text`（OCR 把整条连读）→ 用 `click_ocr_right(cancel_result, live_cancel_offset, "取消(直播广告-右边缘)")`。
     - 否则 `click_ocr_center(cancel_result, "取消(直播广告)")`。
     - `live_ads_cancelled += 1`，置 `_live_cancelled_this_ad=True`，`sleep(1.0)`，return。
   - 若只有「取消」而无「进入直播间」→ 认为非直播弹窗，忽略。
4. **优先级2 完成**：先在 `region_top_right` 找 `"领取成功"`，找不到再全屏找。命中 → 日志「广告观看完成！...」，`click_ocr_right(success_result, x_button_offset, "X按钮(关闭广告)")`，置 `REWARD_POPUP`、重置计时，`sleep(1.5)`，return。
5. **优先级3 倒计时日志**：查 `"秒后可领取"` 输出等待日志；否则根据 `_live_cancelled_this_ad` 输出不同提示。
6. `sleep(scan_interval)`。（**不做健全性检查**，仅靠超时与「领取成功」驱动。）

### 11.4 `_handle_live_room`（直播间内，误入后退出）

1. 超时检查：超 `live_room_timeout` → 置 `IDLE`。
2. 检测是否仍在大直播：`still_in_live = find_text("说点什么") or find_text("本场点赞")`。
3. **若已不在直播间**（已退出画面）：
   - 优先检测推荐弹窗：`find_text("退出不看了")`，没有则 `find_text("不看了")`。命中 → 日志「检测到'退出不看了'推荐弹窗，点击退出...」，`click_ocr_center`，`sleep(1.5)`，**return（留在 LIVE_ROOM，下轮重检）**。
   - 否则 `find_text("领取成功") or find_text("继续观看")` → 切 `AD_PLAYING` 并重置计时。
   - 否则 → 日志「退出直播间，回到空闲状态」置 `IDLE`。
   - return。
4. **仍在直播间**：尝试点右上角 X。定位方法按优先级：
   - **方法1 观众数**：遍历 OCR 结果，找「纯数字、3~5 位、且中心 y < 图高 15%」的一条 → 取 `right` 加 `live_x_viewer_offset`。
   - **方法2 关注**：`find_text("关注")` 未命中方法1时用，取 `right` 加 `live_x_follow_offset`。
   - **方法3 固定位置**：两者都没命中时，用 `w*live_x_fixed_x_ratio, h*live_x_fixed_y_ratio`。
   - 点击绝对坐标 = `left + 偏移` / `top + 偏移`。点击后在 `live_rooms_exited += 1`，`sleep(2.0)` 等待退出动画。

### 11.5 `_handle_reward_popup`（奖励弹窗）

1. `find_text("领取奖励")` 命中 → 日志「找到'领取奖励'按钮，点击领取，准备进入下一个广告」，`click_ocr_center`，`ads_watched += 1`，打印 `>>> 已完成第 N 个广告，等待下一个广告加载 <<<`，置 `AD_PLAYING`、重置计时、`_live_cancelled_this_ad=False`，`sleep(ad_load_wait)`，return。
2. 超时：超 `reward_timeout` → 置 `IDLE`。
3. 否则日志「奖励弹窗中...等待'领取奖励'按钮出现 (已等待 Xs)」，`sleep(scan_interval)`。

### 控制方法

- `stop()`：`running=False, paused=False`，日志「正在停止...」。
- `pause()`：`paused=True`，日志「脚本已暂停」。
- `resume()`：`paused=False`，日志「脚本已恢复运行」。

统计 `stats = {ads_watched, live_ads_cancelled, live_rooms_exited, errors, start_time}`。

---

## 12. 关键 OCR 关键词清单

| 用途 | 关键词（含包含匹配） | 状态触发 |
|:---|:---|:---|
| 入口按钮（主） | `领时长` | IDLE→AD_PLAYING |
| 入口按钮（备） | `继续领` | IDLE→AD_PLAYING |
| 广告完成 | `领取成功` | →REWARD_POPUP/AD_PLAYING |
| 奖励弹窗领取 | `领取奖励` | REWARD_POPUP 点击 |
| 广告倒计时 | `秒后可领取` | 仅日志 |
| 倒计时存在 | `可领取` / `秒后` | IDLE→AD_PLAYING |
| 直播倒计时取消 | `取消` + `进入直播间`（均须在 region_center） | AD_PLAYING 点「取消」 |
| 直播间特征 | `说点什么` / `本场点赞` | →LIVE_ROOM |
| 直播间附加特征 | `关注` / `直播中` | 判定误入直播间 |
| 直播间 X 定位 | 纯数字3~5位（顶部15%） | 点X方法1 |
| 直播间 X 定位 | `关注` | 点X方法2 |
| 推荐弹窗退出 | `退出不看了` / `不看了` | LIVE_ROOM 内点退出 |
| 解锁弹窗主按钮1 | `继续观看` | `坚持退出`弹窗 |
| 解锁弹窗主按钮2 | `领取奖励` | `坚持退出`弹窗 |
| 解锁弹窗特征 | `坚持退出` | 唯一触发特征 |

注意：所有查找均为「双向包含匹配」（`target in text or text in target`），以容忍 OCR 识别偏差。

---

## 13. 点击位置策略详解

所有屏幕绝对坐标 = `窗口左上角(left,top) + OCR结果坐标`。

| 点击场景 | 定位方式 | 默认参数 |
|:---|:---|:---|
| 入口按钮 | `click_ocr_center`（文字中心） | — |
| 「领取成功」右侧 X | `click_ocr_right` 文本右缘 + `x_button_offset` | `-5` |
| 直播「取消」 | 整条连读时点右缘+`live_cancel_offset`，否则点中心 | `-10` |
| 直播间 X（观众数法） | 观众数文本右缘 + `live_x_viewer_offset` | `40` |
| 直播间 X（关注法） | 「关注」右缘 + `live_x_follow_offset` | `200` |
| 直播间 X（兜底） | `w*live_x_fixed_x_ratio, h*live_x_fixed_y_ratio` | `0.90, 0.10` |
| 解锁弹窗主按钮 | `click_ocr_center` | — |
| 推荐弹窗「退出不看了」 | `click_ocr_center` | — |

`click_at` 在 `dry_run` 时不真实点击，仅打印 `[试运行] 点击`。真实点击后若 `debug` 开启，会在 `last_screenshot` 上画绿色十字与圆圈标注并保存到 `debug_screenshots/`。

---

## 14. 调试与测试功能

- **`save_debug`**：把当前截图标注红色框和置信度文字，写入 `{HHMMSS_mmm}_{state}_{tag}.png`；同时写同名 `.txt`（状态、窗口位置、图片尺寸、全部 OCR 结果）。
- **`_save_click_marker`**：点击后画绿色十字/圆圈 + `CLICK:` 标注，写入 `{ts}_CLICK_{state}.png` 和 `.txt`。
- **`run_test_mode`**：截图 + OCR，全量打印识别文字、坐标、置信度，保存 `debug_screenshots/test_result.png` 供人工核对。
- **`run_list_windows_mode`**：列出所有可见窗口 `[hwnd] WxH title`。

---

## 15. GUI 控制面板（PyQt5）架构

### 15.1 线程模型

- `WatcherThread(QThread)`：`run()` 里 `core.AdWatcher(config).run()`。安装 `QtLogHandler`（日志 handler）将日志转发，通过 `log_signal = pyqtSignal(str,str)` 传给主线程。结束发 `finished_signal(dict stats)`。提供 `stop/pause/resume/get_stats`。
- `TestThread(QThread)`：跑 `run_test_mode` 或 `run_list_windows_mode`，同样转发日志。
- 任何操作 `core.logger`、UI 回显都通过 Qt 信号跨线程安全完成。

### 15.2 QtLogHandler

```python
class QtLogHandler(logging.Handler):
    def emit(self, record):
        msg = self.format(record); self.callback(msg, record.levelname)
```
在 `WatcherThread(_emit_log)` 里 `log_signal.emit(msg, level)`。

### 15.3 配置持久化

- `CONFIG_FILE = 同目录/gui_config.json`。
- `save_config_to_file(dict)` / `load_config_from_file()`：JSON `ensure_ascii=False, indent=2`。
- GUI 启动时 `_load_config_to_ui` 把已存配置回填控件；点「开始」时 `_build_config` 读取控件生成 `core.Config` 并同时 `_save_current_config`。

### 15.4 界面布局（QishuiGUI / QMainWindow，默认 1000x780）

- **顶部控制栏**（QHBoxLayout）：开始运行(绿)、暂停/恢复、停止(红)、保存配置、测试截图+OCR、列出窗口、状态标签。
- **主体 QSplitter（水平）**：
  - 左侧「配置面板」（QScrollArea）：
    - **基本设置**：window_title / scan_interval / max_ads / ad_timeout / ad_load_wait / reward_timeout / click_delay。
    - **视频广告点击位置**：x_button_offset。
    - **直播广告点击位置**：live_cancel_offset。
    - **直播间退出点击位置**：live_x_viewer_offset / live_x_follow_offset / live_x_fixed_x_ratio / live_x_fixed_y_ratio / live_room_timeout。
    - **其他选项**：复选框 `保存调试截图`(debug)、`试运行模式（只检测不点击）`(dry_run)。
  - 右侧「运行日志」（QTextEdit，深色背景 Consolas）：INFO/WARNING/ERROR/DEBUG 配色，超 1000 行截断，附「清空日志」按钮。
- **底部统计** QGroupBox：实时展示 广告/取消直播/退出直播间/错误/运行秒数（QTimer 每 1000ms 刷新）。

`ConfigRow` 控件封装了「标签+输入框+提示」三态，按 `input_type`（str/int/double）创建 `QLineEdit/QSpinBox/QDoubleSpinBox`，并负责 `apply_to_config`/`get_value`/`set_value`。

窗口 `closeEvent`：若脚本运行中弹确认框，Yes 则 `stop()+wait(3000)` 后关闭。

入口 `main()`：`QApplication` + `app.setStyle("Fusion")` + `QishuiGUI().show()` + `app.exec_()`。

---

## 16. 启动批处理规范

`启动GUI.bat` 必须：
1. **UTF-8 编码且带 BOM、CRLF 换行**（LF 会解析报错）。
2. 首行 `@echo off`，第二行 `chcp 65001 >nul 2>&1`。
3. `cd /d "%~dp0"` 到脚本目录。
4. 显式指定 Python 绝对路径（本机为 `C:\Users\Administrator\AppData\Roaming\TRAE SOLO CN\ModularData\ai-agent\vm\tools\python\python.exe`）并加 `-X utf8`。
5. `title` 设置窗口标题。
6. 运行 `qishui_gui.py`；`if errorlevel 1` 时打印依赖提示并 `pause`。

模板：
```bat
@echo off
chcp 65001 >nul 2>&1
cd /d "%~dp0"
set "PYTHON=C:\...\python.exe"
title 汽水音乐自动看广告 - GUI
"%PYTHON%" -X utf8 qishui_gui.py
if errorlevel 1 (
    echo.
    echo 运行出错，请检查依赖是否安装完整
    echo 可运行: pip install PyQt5 rapidocr-onnxruntime pywin32 keyboard mss Pillow numpy
    pause
)
```

> 踩坑：若 bat 用 LF (Unix) 换行，cmd 会把 `set "PYTHON=...` 截断（报 `'HON' 不是内部或外部命令`）。必须 CRLF。

---

## 17. 命令行接口

`qishui_ad_watcher.py` 的 `parse_args()`（手动解析 `sys.argv`，先解析配置再处理模式）：

| 参数 | 作用 |
|:---|:---|
| `--window 关键词` | 设置 `window_title` |
| `--interval 秒` | 设置 `scan_interval` |
| `--max-ads N` | 设置 `max_ads` |
| `--debug` | 开调试截图 |
| `--no-debug` | 关调试截图 |
| `--dry-run` | 试运行（不点击） |
| `--list-windows` | 列出窗口后退出 |
| `--test` | 测试模式（截图+OCR）后退出 |

`main()`：解析配置 → `AdWatcher(config)` → 若支持 keyboard 注册 `esc` 热键调 `stop()` → `watcher.run()`；捕获 `KeyboardInterrupt` 调 `stop()`。

---

## 18. 已踩坑的兼容性要点

1. **投屏窗口截图**必须用 mss 前台截图；`PrintWindow` 抓不到视频流，会得到空白。截图前要把目标窗口 `SetForegroundWindow` 置前。
2. **OCR 常把「4s后进入直播间取消」连读成一行**，点中心要点到「进入直播间」── 必须点右边缘 + 负偏移。判断依据 `"进入直播间" in text or "后进入" in text`。
3. **观众数定位 X**：先找顶部 15% 区域内 3~5 位纯数字，X 在其右侧 `live_x_viewer_offset`；不行再找「关注」右侧；最后用固定比例兜底。
4. **退出直播间的推荐弹窗**：已退出画面但出现「退出不看了/不看了」时要点它，否则卡死。
5. **「坚持退出」解锁弹窗**：以「坚持退出」为唯一特征触发，主按钮优先「继续观看」否则「领取奖励」，避免与正常「领取奖励」混淆。
6. **窗口误匹配**：关键词别填「汽水」，否则会锁到本程序 GUI；`find_target_window` 自动排除含「控制面板/命令提示符/PowerShell/cmd.exe/Terminal」的自身/终端窗口。
7. **bat 必须 CRLF+UTF8 BOM**，否则 `set` 命令被截断。
8. **DPI 感知**必须在开头调用，否则高分屏坐标偏移。
9. **键盘热键与 GUI 线程**：ESC 热键与 GUI 共用核心 `logger`；GUI 用 Qt 信号做线程安全日志回显。
10. RapidOCR / keyboard 缺失时优雅降级，不崩溃。

---

## 19. 可执行文件打包（PyInstaller）

目标产物为一个**文件夹（onedir 模式）**，启动程序是 `exe`，依赖 dll/OCR 模型/PyQt5 都在同目录：

```bat
pyinstaller --windowed --collect-all rapidocr_onnxruntime --name 汽水音乐自动看广告 qishui_gui.py
```

要点：
- 用 `--collect-all rapidocr_onnxruntime` 把 OCR 及模型/onnxruntime 完整带出。
- `--windowed`（免控制台窗口）。
- 打包后发布**整个 `dist/<名字>/` 文件夹**，不要只发 exe。
- 产物很大（约 300MB 以上，onnxruntime+numpy+PyQt5+模型），压缩后约 100-130MB。
- 打包机需 **Windows x64 + Python 3.10/3.11**；目标机 Windows 10/11 64 位即可，无需安装 Python。

> 若打包后闪退，通常因 RapidOCR 模型未被打入，需用额外的 `--hidden-import`/`--collect-data` 补齐。

---

## 20. 测试验证清单

1. `pip install -r requirements.txt` 成功。
2. `python qishui_ad_watcher.py --list-windows` 能列出投屏窗口。
3. `python qishui_ad_watcher.py --test` 能截图并打印 OCR 结果，`debug_screenshots/test_result.png` 标注正确、能看清「领时长」等关键文字。
4. 启动 GUI，把「窗口标题关键词」设为 `投屏`，「列出窗口」能查到投屏窗口。
5. 「测试截图+OCR」在日志区显示投屏内容的 OCR 文字。
6. 点「开始运行」：能自动点「领时长/继续领」→ 广告播放。
7. 视频广告：倒计时结束 → 自动点「领取成功」右侧 X → 点「领取奖励」→ 进入下一广告，计数递增。
8. 直播广告：出现「取消」后快速点中（右边缘），不进入直播间。
9. 若误入直播间：能定位右上角 X 退出，出现推荐弹窗时能点「退出不看了」。
10. 「坚持退出」解锁弹窗出现时能点「继续观看/领取奖励」。
11. 设 `max_ads=1`：完成后自动停止。
12. 「暂停/恢复」生效；关闭窗口时若在运行有确认提示。
13. debug 模式在 `debug_screenshots/` 产生标注截图与点击十字标记。
14. 打包 exe 后，目标机双击能打开 GUI 且功能正常。

---

## 21. 重写完备性检查清单

实现完成后逐项核对，缺一不可：

- [ ] `Config` 含全部字段且默认值与本文档一致
- [ ] `OCREngine.detect` 返回 `text/x/y/w/h/center/right/score`
- [ ] `find_text` 与 `find_text_in_region` 采用双向包含匹配
- [ ] 两个截图函数与 `capture_window`（mss + 置前）
- [ ] `AdWatcher.find_target_window` 含自身窗口排除 + 选最大面积
- [ ] 状态常量四种，`tick` 中「上限检查→截图OCR→坚持退出弹窗→状态分发」顺序正确
- [ ] `_handle_continue_watching_popup` 以「坚持退出」为特征，主按钮优先「继续观看」再「领取奖励」
- [ ] 空闲态入口兼容「领时长」「继续领」
- [ ] 广告播放态：误入直播间检测 → 直播取消（region_center 内「取消」+「进入直播间」）→ 「领取成功」右侧X → 倒计时日志
- [ ] 直播取消时区分连读整体（点右缘）与独立「取消」（点中心）
- [ ] 直播间态：超时 / 仍在大直播判定 / 「退出不看了」推荐弹窗 / 三种点 X 定位（观众数→关注→固定比例）
- [ ] 奖励弹窗态：点「领取奖励」、计数、超时回空闲
- [ ] `stop/pause/resume`、`stats` 五字段
- [ ] `run_test_mode`、`run_list_windows_mode` 两个工具模式
- [ ] 命令行全参数解析
- [ ] GUI（PyQt5）含全部配置组、日志、统计、线程模型、配置持久化
- [ ] `启动GUI.bat` 为 UTF-8(BOM)+CRLF、chcp 65001、显式 python 路径、errorlevel 提示
- [ ] `requirements.txt` 七项依赖
- [ ] Windows 下 `SetProcessDPIAware()` 在开头调用
- [ ] RapidOCR/keyboard 缺失时降级不崩溃